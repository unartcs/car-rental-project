{
  "name": "car-rental-project",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://unartcs.github.io/car-rental-project",
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "@tinacms/cli": "^1.5.16",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "react-scroll": "^1.8.9",
    "tinacms": "^1.5.8",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "tinacms build && react-scripts start",
    "build": "tinacms build && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "dev": "tinacms dev -c \"next dev\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}